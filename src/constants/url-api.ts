const APIPORTAL = '/api/portal';

const ENDPOINTS_PORTAL = {
  MENU_NAVIGATION: `${APIPORTAL}/menu/navigation`,
  FOOTER: `${APIPORTAL}/footer`,
  DETAIL_NEWS: `${APIPORTAL}/news`, // /api/portal/news/{id}
};

export { ENDPOINTS_PORTAL };
