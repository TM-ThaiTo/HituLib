const BACKEND_URL = process.env.BACKEND_URL;

const APIPORTAL = '/api/portal';

const ENDPOINTS_PORTAL = {
    MENU_NAVIGATION: `${BACKEND_URL}${APIPORTAL}/menu/navigation`,
}

export {
    ENDPOINTS_PORTAL
};
